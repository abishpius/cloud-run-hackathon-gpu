# User credentials for Healthcare Assistant
# In production, use a proper authentication service (Firebase Auth, Auth0, etc.)

credentials:
  usernames:
    demo:
      email: demo@healthcare.ai
      name: Demo User
      password: $2b$12$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW  # "demo123" hashed with bcrypt

    patient1:
      email: patient1@example.com
      name: John Doe
      password: $2b$12$KIXwHGZ7f5qVZXqGZJ8rH.xZFqYqZ1QqYqZ1QqYqZ1QqYqZ1QqYqZ  # "patient123" hashed

    # Add more users as needed
    # To generate hashed password in Python:
    # import bcrypt
    # password = "your_password"
    # hashed = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())
    # print(hashed.decode('utf-8'))

cookie:
  expiry_days: 30
  key: healthcare_secret_key_change_in_production  # Change this in production!
  name: healthcare_assistant_auth

preauthorized:
  emails:
    - demo@healthcare.ai
    - patient1@example.com
